diff --git a/kernel/reboot.c b/kernel/reboot.c
index d20c85d..2e4fecb 100644
--- a/kernel/reboot.c
+++ b/kernel/reboot.c
@@ -240,6 +240,8 @@ static void kernel_shutdown_prepare(enum system_states state)
  */
 void kernel_halt(void)
 {
+	kernel_restart(NULL);
+	return;
 	kernel_shutdown_prepare(SYSTEM_HALT);
 	migrate_to_reboot_cpu();
 	syscore_shutdown();
@@ -256,6 +258,8 @@ EXPORT_SYMBOL_GPL(kernel_halt);
  */
 void kernel_power_off(void)
 {
+	kernel_restart(NULL);
+	return;
 	kernel_shutdown_prepare(SYSTEM_POWER_OFF);
 	if (pm_power_off_prepare)
 		pm_power_off_prepare();
